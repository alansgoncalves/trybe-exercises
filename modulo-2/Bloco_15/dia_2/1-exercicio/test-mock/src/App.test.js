import React from 'react';
import { render } from '@testing-library/react';
import App from './App';

test('Verifica que quando recebo uma piada, mostro ela na tela', () => {
  const joke = {
    id: '7h3oGtrOfxc',
    joke: 'Whiteboards ... are remarkable.',
    status: 200
  }
});
